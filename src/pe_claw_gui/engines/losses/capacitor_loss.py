"""Capacitor-loss helpers."""

from __future__ import annotations


def estimate_capacitor_loss(device: dict, operating_point: dict) -> float:
    """Return the current placeholder capacitor loss."""
    _ = device, operating_point
    return 0.0
