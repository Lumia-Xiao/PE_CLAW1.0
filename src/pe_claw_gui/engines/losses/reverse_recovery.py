"""Reverse-recovery-loss helpers."""

from __future__ import annotations


def estimate_reverse_recovery_loss(device: dict, operating_point: dict) -> float:
    """Return the current placeholder reverse-recovery loss."""
    _ = device, operating_point
    return 0.0
