"""Switching-loss helpers."""

from __future__ import annotations


def estimate_switching_loss(device: dict, operating_point: dict) -> float:
    """Return the current placeholder switching loss."""
    _ = device, operating_point
    return 0.0
